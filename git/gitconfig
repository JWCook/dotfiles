[user]
    name = JWCook
    email = JCook83@gmail.com
[github]
    user = JWCook
[core]
    editor = vim
[alias]
    amend = commit --amend
    c = commit --verbose
    cm = commit -m
    down = pull
    f = fetch origin master:master
    p = pull
    pr = "!git stash; git pull --rebase; git stash pop"
    patch = add --patch
    rb = git rebase --interactive master
    s = status
    stat = shortlog --summary --numbered
    up = push
[push]
    default = simple
[merge "ours"]
    driver = true
[color]
    diff = always
    grep = always
    status = always
[color "diff"]
    meta = blue
[color "status"]
    header = blue
    branch = cyan
    added = green
    changed = cyan
    deleted = red
    untracked = magenta
[tig]
    vertical-split = false
    split-view-height = 80%
    tab-size = 4
    line-graphics = utf-8
[credential]
    helper = cache --timeout=3600
